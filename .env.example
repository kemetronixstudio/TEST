# Optional: replace the default built-in admins used by the backend API
BUILTIN_ADMINS_JSON=[{"user":"Dr. Tarek","pass":"T01032188008"},{"user":"HITMAN","pass":"01002439054"}]

# Required for secure signed admin API sessions
ACCESS_ACCOUNTS_SESSION_SECRET=replace-with-a-long-random-secret

# Optional: persist access accounts in Vercel KV (recommended for Vercel deployments)
KV_REST_API_URL=
KV_REST_API_TOKEN=
ACCESS_ACCOUNTS_STORAGE_KEY=kg:access_accounts:v1

# Optional local/dev storage path when not using KV
ACCESS_ACCOUNTS_DATA_PATH=./data/access-accounts.json
